给定一篇文本，给定一个n行m列的矩形格子，把26个字母放到若干个方格中，使得打字输入这篇文本时，手指移动距离最短（人有两个手）。
给定一篇文本，给定一个n行m列的矩形格子，把26个字母放到若干个方格中，使得打字输入这篇文本时，手指移动距离最长（人有两个手），目的是让手得到充分的运动，手就不会酸痛。

多环旅行商问题，在一个图中画3个圈，使得这三个圈覆盖全部点，要求3个圈的长度值和最小。
变形：要求3个圈的最长的那个圈长度最小。